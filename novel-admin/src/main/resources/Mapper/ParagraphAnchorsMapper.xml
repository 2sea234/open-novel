<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhy.mapper.ParagraphAnchorsMapper">

    <insert id="insertBatchParagraphAnchors" useGeneratedKeys="true" keyProperty="anchorId">
        insert into paragraph_anchors
        (article_id, chapter_index, chapter_title, paragraph_index, anchor_hash, occurrence_no, paragraph_id)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.articleId}, #{item.chapterIndex}, #{item.chapterTitle},
            #{item.paragraphIndex}, #{item.anchorHash}, #{item.occurrenceNo}, #{item.paragraphId})
        </foreach>
    </insert>

</mapper>