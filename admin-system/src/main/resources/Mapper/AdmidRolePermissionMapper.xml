<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhy.admin.mapper.AdminRolePermissionMapper">

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        select permission_id
        from admin_role_permission
        where role_id = #{roleId}
    </select>

    <delete id="deletePermissionsByRoleId">
        delete from admin_role_permission
        where role_id = #{roleId}
    </delete>

    <insert id="batchInsertRolePermissions">
        insert into admin_role_permission
        (
        role_id,
        permission_id,
        create_by,
        create_time
        )
        values
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (
            #{roleId},
            #{permissionId},
            #{adminId},
            now()
            )
        </foreach>
    </insert>


</mapper>